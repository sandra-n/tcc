#For now fill the following variables with your own tokens with elevated access, at least
#TO-DO: develop or use cloud functions so that twitter keys will be previously set

api_Key = '<Consumer API KEY>'

api_key_secret = '<Consumer API key secret>'

bearer_token = '<Authentication bearer token>'

access_token = '<Authentication access token>'

access_token_secret = '<Authentication access token secret>'
